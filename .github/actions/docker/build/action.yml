name: Build Docker Images
description: Build Docker images using Makefile and clean up MySQL conflicts

inputs:
  image_tag:
    description: Custom image tag to apply (e.g., dev-213...)
    required: false

runs:
  using: "composite"
  steps:
    - name: 📁 Ensure Build Cache Directory Exists
      run: mkdir -p /tmp/.buildx-cache
      shell: bash

    - name: 💾 Restore Docker Build Cache
      uses: actions/cache@v4
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: 🧼 Kill Conflicting MySQL Containers
      run: |
        docker ps -q --filter expose=3306 | xargs -r docker rm -f || true
      shell: bash

    - name: 🏗️ Build Docker Image with Optional Tag
      run: |
        echo "CI=${CI}"
        make down || true
        make build

        IMAGE_ID=$(docker images -q | head -n 1)
        if [ -n "$IMAGE_ID" ]; then
          docker tag "$IMAGE_ID" backend-api:latest
          echo "✅ Tagged $IMAGE_ID as backend-api:latest"

          if [ -n "${{ inputs.image_tag }}" ]; then
            docker tag "$IMAGE_ID" backend-api:${{ inputs.image_tag }}
            echo "✅ Also tagged as backend-api:${{ inputs.image_tag }}"
          fi
        else
          echo "❌ No image found to tag"
          exit 1
        fi
      shell: bash
      env:
        CI: true
        DOCKER_BUILDKIT: 0

    - name: 🐳 List Docker Images (Debug)
      run: docker images
      shell: bash

    - name: ⏳ Wait for Services to Initialize
      run: sleep 20
      shell: bash
